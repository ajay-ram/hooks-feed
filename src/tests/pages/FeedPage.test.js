import { render } from '@testing-library/react';
import FeedsPage from '../../pages/FeedsPage';

describe ("FeedPage stories",()=>{
    it('renders without crashing', () => {
        // const {container} = render(<FeedsPage 
        //                         />);
        expect(true).toBe(true);
    });
});
